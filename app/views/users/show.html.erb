 <div class="page-header">
  <h1>Member Profile:<br><small><%= @user.email %></small></h1>
</div>


<% @user.jobs.each do |job| %>
      <ul>
        <li><%= link_to job.title, job %></li>
        <%#= link_to job.user.email, job.user %>
      </ul>

      <% end %>
<br>
<%= link_to 'Back to Jboard', jobs_path %>